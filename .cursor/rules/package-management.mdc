---
description: Package management
globs: 
alwaysApply: false
---
# Package management

- Always use `uv`
- Never use `pip`
- Use `uv add ...` to add new dependencies, don't directly edit the project.toml file.